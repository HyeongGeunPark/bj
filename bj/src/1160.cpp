/*
bj1160 Random Number Generator

일반항을 구하면 결국 거듭제곱을 빨리 구하는 방법으로 귀결된다.

*/

#include<iostream>

using ull = unsigned long long;

ull modpow(ull base, int power, ull mod)
{
	ull result = 1;
	ull temp = 1;
	while (power) {

	}
}

int main(void) {
	ull m, a, c, x0, n, g;
	//std::cin >> m >> a >> c >> x0 >> n >> g;

	std::cout << sizeof(ull);
}