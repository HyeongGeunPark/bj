#include <iostream>
#include <unordered_map>
#include <vector>

int main(void) {
  std::cin.tie(0);
  std::ios_base::sync_with_stdio(0);
  int n;
  std::cin >> n;

  std::vector<int> a(n);
  std::vector<int> ans(n);
  std::unordered_map<int, int> constraints;
  for (int i = 0; i < n; ++i) {
    std::cin >> a[i];
  }

  for (int i = 0; i < n; ++i) {
  }
}